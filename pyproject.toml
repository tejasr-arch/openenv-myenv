[project]
name = "openenv-myenv"
version = "0.1.0"
description = "Simple RL environment for resume scoring"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"